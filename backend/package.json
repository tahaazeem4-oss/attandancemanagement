{
  "name": "attendance-backend",
  "version": "1.0.0",
  "description": "School Attendance Management System — Node.js Backend",
  "main": "server.js",
  "scripts": {
    "prestart": "powershell -NoProfile -Command \"$p = (Get-NetTCPConnection -LocalPort 5000 -State Listen -ErrorAction SilentlyContinue).OwningProcess; if ($p) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue }\"",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "kill": "powershell -Command \"Stop-Process -Id (Get-NetTCPConnection -LocalPort 5000 -State Listen).OwningProcess -Force 2>$null; Write-Host 'Port freed'\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.9.7",
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
